import { autoEvalsNode } from './AutoEvalsNode.js';
export const autoevalsPlugin = {
    id: 'autoevals',
    name: 'Autoevals',
    register: (register) => {
        register(autoEvalsNode);
    },
};
